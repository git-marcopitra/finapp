import { FC } from "react";
import { Header, Layout,Balance } from "../components";

const HomeScreen: FC = () => (
  <Layout>
    <Header />
    <Balance />
  </Layout>
);

export default HomeScreen;
